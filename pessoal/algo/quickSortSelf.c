#include <stdio.h>

int quick_sort(int array[], int low, int high);

int main(void)
{

    int size = 0;
    int nums[10] = {0};
    printf("Digite 10 números: ");
    for (int i = 0; i < 10; i++)
    {
        scanf("%d", nums[i]);
    }

    return 0;
}

int quick_sort(int array[], int low, int high)
{

}